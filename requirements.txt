torch>=2.0
torchvision>=0.15
lightning>=2.2
jsonargparse[signatures]>=4.27
numpy
scikit-learn
matplotlib
corner

# Only needed to read the real JetClass ROOT files; the JetClass DataModules
# fall back to a synthetic generator when these (or the files) are absent.
uproot
awkward
