# JetClass supervised baseline: DeepSets backbone + classifier, cross-entropy.
#   python cli.py fit --config configs/jetclass_supervised.yaml
trainer:
  accelerator: auto
  default_root_dir: runs/jetclass_supervised/
  max_epochs: 3
  log_every_n_steps: 10
  callbacks:
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        monitor: val/loss
        mode: min
        save_last: true
  logger:
    class_path: lightning.pytorch.loggers.CSVLogger
    init_args:
      save_dir: runs/jetclass_supervised/

model:
  class_path: models.litmodels.SupervisedModule
  init_args:
    model:
      class_path: models.networks.SupervisedJetNet
      init_args:
        input_dim: 7
        emb_dim: 16
        n_classes: 10
        encoder: deepsets
    lr: 1.0e-3

data:
  class_path: data.datasets.JetClassDataModule
  init_args:
    batch_size: 256
    num_workers: 1
